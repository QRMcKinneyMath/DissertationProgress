\newcommand{\LowerBound}[0]{\textbf{\hyperref[def:LowerBound]{Lower Bound}}\xspace}
\newcommand{\BoundedFromBelow}[0]{\textbf{\hyperref[def:LowerBound]{Bounded From Below}}\xspace}
\newcommand{\LowerBounds}[0]{\textbf{\hyperref[def:LowerBound]{Lower Bounds}}\xspace}
\newcommand{\LB}[0]{\textbf{\hyperref[def:LowerBound]{LowerBound}}\xspace}

\begin{df}[\LowerBound]
\label{def:LowerBound}
\rm
    Let $X$ be a nonempty set.
    Let $R$ be a \Relation on X. 
    Let $Y \subset X$.
    Let $a \in X$. 
    We say that $a$ is an 
    \LowerBound for $Y$ if
    for every $x \in Y$, 
    we have $a R x$. 
    If $a$ is an \LowerBound
    for $Y$,
    then we say that 
    the set Y is \BoundedFromBelow
    by a. 
	We denote the set of \LowerBounds of 
	$Y$ with respect to the relation $R$ with
	$\LB_R(Y)$. 
	When $R$ is understood, we denote this set with
	$\LB(Y)$. 
\end{df}
