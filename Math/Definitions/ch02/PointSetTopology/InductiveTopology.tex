\newcommand{\InductiveTopology}[0]{\textbf{\hyperref[def:InductiveTopology]{Inductive Topology}}\xspace}
\newcommand{\InductiveTopologies}[0]{\textbf{\hyperref[def:InductiveTopology]{Inductive Topologies}}\xspace}

\begin{df}[\InductiveTopology]
\label{def:InductiveTopology}

\rm
    Let $X$ be a set and for each 
    $\alpha \in A$, let 
    $(Y_\alpha, \T_\alpha)$ be a 
    \TopologicalSpace.
    Furthermore, for each $\alpha \in A$, let 
    $\phi_\alpha : (Y, \T_\alpha) \to X$. 
    Let $\T$ be the 
    \TopologyFinest
    topology on $X$ for which 
    each $\phi_\alpha$ is 
    \ContinuousFunction. 
    We call $\T$ the \InductiveTopology
    on $X$ induced by $\{\phi_\alpha\}_{\alpha \in A}$. 
\end{df}
