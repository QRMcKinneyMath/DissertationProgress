\newcommand{\TopologicalGroup}[0]{ \textbf{\hyperref[def:TopologicalGroup]{Topological Group}}\xspace}
\begin{df}[\TopologicalGroup]

\rm
\label{def:TopologicalGroup}
    Let $(G,+,e)$ be a \Group.
    Let $\T$ be a 
    \Topology on
    $G$ such that 
    $+:G \times G \to G$
	and the 
    \GroupInverseOperator
    are both
    \ContinuousFunction
    with respect to the 
	\ProductTopology.
    In this scenario, we call $(G, \T)$ a \TopologicalGroup.
\end{df}
