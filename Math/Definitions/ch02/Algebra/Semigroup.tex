\label{def:Semigroup}
\newcommand{\Semigroup}[0]{
    \bf \hyperref[def:Semigroup]{Semigroup} \rm
}
\newcommand{\Semigroups}[0]{
    \bf \hyperref[def:Semigroup]{Semigroups} \rm
}
\newcommand{\CommutativeSemigroup}[0]{
    \bf \hyperref[def:Semigroup]{Commutative Semigroup} \rm
}
\newcommand{\CommutativeSemigroups}[0]{
    \bf \hyperref[def:Semigroup]{Commutative Semigroups} \rm
}

\begin{df}[\Semigroup]
    Let $(X,\oplus)$ be a 
    \Magma.
    Let $\oplus$ 
    be 
    \AssociativeFunction.
    Then we say that 
    $(X,\oplus)$ 
    is a 
    \Semigroup. 
\end{df}
