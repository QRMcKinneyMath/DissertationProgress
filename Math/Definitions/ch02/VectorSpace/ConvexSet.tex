\newcommand{\ConvexSet}[0]{
    \bf \hyperref[def:ConvexSet]{Convex} \rm 
}

\begin{df}[\ConvexSet]
\label{def:ConvexSet}
\rm
    Let $V$ 
    be a 
    \VectorSpace
    over a $\Field$
    $\F \in \{\R, \C\}$. 
    Let 
    $K \subset V$. 
    We say that 
    $K$ is
    \ConvexSet
    if 
    for every pair $x,y \in K$, 
    we have $[x,y] \subset K$.
\end{df}
