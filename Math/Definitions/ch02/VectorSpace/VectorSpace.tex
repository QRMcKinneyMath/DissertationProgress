\label{def:vectorspace}
\newcommand{\VectorSpace}[0]{
    \bf \hyperref[def:vectorspace]{Vector Space} \rm
}

\newcommand{\VectorSpaces}[0]{
    \bf \hyperref[def:vectorspace]{Vector Spaces} \rm
}

\newcommand{\Field}[0]{
    \bf \hyperref[def:vectorspace]{Field} \rm
}

\newcommand{\Fields}[0]{
        \bf \hyperref[def:vectorspace]{Fields} \rm
}

\newcommand{\VectorSubspace}[0]{
    \bf \hyperref[def:vectorspace]{Vector Subspace} \rm
}
\newcommand{\VectorSubspaces}[0]{
    \bf \hyperref[def:vectorspace]{Vector Subspaces} \rm
}
