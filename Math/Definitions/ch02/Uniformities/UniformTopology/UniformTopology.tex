\newcommand{\UniformTopology}[0]{
    \textbf{\hyperref[def:UniformTopology]{Uniform Topology}}
}
\newcommand{\UniformTopologies}[0]{
    \textbf{\hyperref[def:UniformTopology]{Uniform Topologies}}
}
\newcommand{\UniformInduced}[0]{
    \textbf{\hyperref[def:UniformTopology]{Induced}}
}
\newcommand{\UniformSpace}[0]{
    \textbf{\hyperref[def:UniformSpace]{Uniform Space}}
}
\newcommand{\UniformSpaces}[0]{
    \textbf{\hyperref[def:UniformSpace]{Uniform Spaces}}
}
\begin{df}[\UniformTopology]
\label{def:UniformTopology}
Let $X \neq \emptyset$ and let 
$\scW$ be a \Uniformity on $X$.
Let $\scT_{\scW}$ be the \Topology defined in
\ref{prop:UniformTopology}.
Then we call $\scT_{\scW}$ the 
\UniformTopology on $X$ 
\UniformInduced by $\scW$. 
We may call $(X, \scW, \scT_{\scW})$ a \UniformSpace.
\end{df}
